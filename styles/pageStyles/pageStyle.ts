type PageStyle = {
  pageStyle: string;
  pageHr: string;
  pageH1: string;
};

const pageStyle: PageStyle = {
  pageStyle:
    "block w-full mt-1 m-auto bg-mainColor pt-5 sm:w-full md:container lg:container xl:container mb-11",
  pageHr: "border-1 border-black",
  pageH1:
    "-text-animation mt-11 text-center xl:text-4xl lg:text-3xl md:text-2xl",
};

export default pageStyle;
